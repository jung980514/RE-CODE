version: '3.8'

services:
  backend:
    image: hj00b/recode-backend:dev-${BUILD_NUMBER:-latest}
    container_name: recode-backend-dev
    ports:
      - "8088:8088"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    image: hj00b/recode-frontend:dev-${BUILD_NUMBER:-latest}
    container_name: recode-frontend-dev
    expose:
      - "3030"
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

  nginx:
    image: hj00b/recode-nginx:dev-${BUILD_NUMBER:-latest}
    container_name: recode-nginx-dev
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - frontend
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge 