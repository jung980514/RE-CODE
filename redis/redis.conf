# 포트 설정
port 6379

# 외부 접근 허용 (보안에 주의)
bind 0.0.0.0

# 보안: 인증 비밀번호 설정 (선택)
requirepass ${REDIS_PASSWORD}

# 데이터 지속성 설정
save 900 1
save 300 10
save 60 10000

# AOF (Append Only File) 설정
appendonly yes

# 메모리 정책 (토큰 만료 기반이라면 volatile-ttl 권장)
maxmemory-policy volatile-ttl

# 로그 레벨
loglevel notice
